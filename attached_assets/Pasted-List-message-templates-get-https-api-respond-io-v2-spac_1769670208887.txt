List message templates
get
https://api.respond.io/v2/space/channel/{id}/template

Get all the message template of a connected channels in the workspace.
Request
Path Parameters
id
string
required

ID of the channel to list template
Query Parameters
limit
integer

Number of items to return i.e. page size.
>= 1< 100
Default:
10
cursorId
integer

Pointer to the template id from where the next batch of records should start from.
Responses
200
400
401
429
500
Body
responses
/
200
items
array[MessageTemplate]
id
integer
Example:
12345
name
string
required
Example:
conversation_follow_up
components
required
bundle
object
channelId
integer
required
Example:
1234
botId
integer
required
Example:
123
languageCode
string
required

Follows ISO 639-1 standard
Example:
en
namespace
string
Default:
null
category
string
required
Example:
MARKETING
status
string
Example:
approved
statusDetail
string
pagination
object
next
string
required
Example:
https://api.respond.io/space/channel/{id}/template?limit=10&cursorId=10
previous
string
required
Example:
https://api.respond.io/space/channel/{id}/template?limit=10&cursorId=-10
Token
:
id*
:
cursorId
:
limit
:

const request = require('request');

const options = {
  method: 'GET',
  url: 'https://api.respond.io/v2/space/channel/{id}/template',
  headers: {Accept: 'application/json', Authorization: 'Bearer 123'}
};

request(options, function (error, response, body) {
  if (error) throw new Error(error);

  console.log(body);
});

{
  "items": [
    {
      "id": 12345,
      "name": "conversation_follow_up",
      "components": [
        {
          "type": "header",
          "format": "text",
          "text": "Header text",
          "parameters": [
            {
              "type": "text",
              "text": "respond.io"
            }
          ]
        }
      ],
      "bundle": {},
      "channelId": 1234,
      "botId": 123,
      "languageCode": "en",
      "namespace": null,
      "category": "MARKETING",
      "status": "approved",
      "statusDetail": "string"
    }
  ],
  "pagination": {
    "next": "https://api.respond.io/space/channel/{id}/template?limit=10&cursorId=10",
    "previous": "https://api.respond.io/space/channel/{id}/template?limit=10&cursorId=-10"
  }
}